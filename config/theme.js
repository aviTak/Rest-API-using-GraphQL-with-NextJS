if (typeof window !== "undefined") {

    window.$light = {
        backgroundColor: '#ffffff',
        color: '#000000'
    };
    
    window.$dark = {
        backgroundColor: '#000000',
        color: '#ffffff'
    };

}
